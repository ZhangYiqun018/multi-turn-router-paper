\begin{lstlisting}[style=prompt]
<tool name="browse">
  <description>Visit webpage(s) and extract information based on a goal</description>
  <parameters>
    <param name="url" type="string" required="true">URL to visit. Can be a single URL or array of URLs.</param>
    <param name="goal" type="string" required="true">The goal of the visit - what information to extract from the webpage(s).</param>
    <param name="extract_mode" type="string" required="false">Content extraction mode (default: text). Only used when LLM summary is disabled.</param>
  </parameters>
</tool>
\end{lstlisting}
